{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "postbuild": "mkdir -p .output/public && cp -r public/. .output/public/ && cp -r node_modules/leaflet/dist/images/. public/"
  },
  "dependencies": {
    "dayjs": "^1.11.13",
    "leaflet": "^1.9.4",
    "mongoose": "^8.12.1",
    "nuxt": "^3.15.4",
    "qrcode": "^1.5.4",
    "vue": "latest",
    "vue-router": "latest",
    "vuetify-nuxt-module": "^0.18.3"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.5",
    "sass-embedded": "^1.85.1"
  }
}
